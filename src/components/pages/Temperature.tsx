import { Text } from '@chakra-ui/react'
import { PagesContainer } from '../parts'

export const Temperature = () => {
  return (
    <PagesContainer>
      <Text fontWeight={"800"}>Temperature</Text>
    </PagesContainer>
  )
}